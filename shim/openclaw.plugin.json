{
  "id": "openclaw-security",
  "name": "OpenClaw Security Platform",
  "description": "Bring-your-own-security evaluator chain for OpenClaw agents",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "evaluationUrl": {
        "type": "string",
        "description": "URL of the Python evaluation server",
        "default": "http://127.0.0.1:9920/evaluate"
      },
      "timeoutMs": {
        "type": "number",
        "description": "Timeout for evaluation requests in milliseconds",
        "default": 3000
      }
    }
  }
}
